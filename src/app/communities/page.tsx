import type{Metadata}from "next";import{CommunityDirectory}from "@/components/communities/community-directory";import{getCommunityDirectory}from "@/data/live-locks";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Football Communities",description:"Find the club communities building transparent football records on LeagueCred."};export default async function CommunitiesPage(){const entries=await getCommunityDirectory();return <main><header className="border-b bg-foreground text-background"><div className="page-shell py-12"><p className="text-xs font-bold tracking-[.16em] text-primary uppercase">Every club. One community.</p><h1 className="mt-4 font-heading text-6xl leading-none font-extrabold uppercase sm:text-8xl">Find your people.</h1><p className="mt-4 max-w-2xl text-lg text-background/70">Browse supporters by club, see who is active, and find the communities building evidence instead of noise.</p></div></header><div className="page-shell py-8 sm:py-12"><CommunityDirectory entries={entries}/></div></main>}
