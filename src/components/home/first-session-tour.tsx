"use client";import{useState,useSyncExternalStore}from"react";import{ArrowRightIcon,RadioIcon,TargetIcon,UsersRoundIcon}from"lucide-react";import{Button}from"@/components/ui/button";import{Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle}from"@/components/ui/dialog";
const subscribe=()=>()=>{};const steps=[{title:"Choose your football home",text:"Tell LeagueCred the club and leagues you genuinely know. That identity connects your calls to the right community.",icon:UsersRoundIcon},{title:"Make one Daily Lock",text:"Choose only the winner you feel strongest about. One selective call builds more trust than ten guesses.",icon:TargetIcon},{title:"Watch the global board",text:"Active locks from every league meet in one place. Read reasons, add an opinion, and vote the discussion up or down.",icon:RadioIcon}];
export function FirstSessionTour(){const first=useSyncExternalStore(subscribe,()=>!localStorage.getItem("leaguecred-home-tour-complete"),()=>false),[dismissed,setDismissed]=useState(false),[step,setStep]=useState(0),open=first&&!dismissed;function close(){localStorage.setItem("leaguecred-home-tour-complete","1");setDismissed(true)}const current=steps[step],Icon=current.icon;return <Dialog open={open} onOpenChange={value=>{if(!value)close()}}><DialogContent className="rounded-none sm:max-w-xl"><DialogHeader><span className="text-xs font-bold uppercase text-primary">Welcome · {step+1} of {steps.length}</span><Icon className="mt-5 size-10 text-primary"/><DialogTitle className="mt-3 font-heading text-4xl font-extrabold uppercase">{current.title}</DialogTitle><DialogDescription className="text-base leading-7">{current.text}</DialogDescription></DialogHeader><DialogFooter><Button variant="ghost" onClick={close}>Skip tour</Button>{step? <Button variant="outline" onClick={()=>setStep(x=>x-1)}>Back</Button>:null}{step===steps.length-1?<Button onClick={close}>Start exploring</Button>:<Button onClick={()=>setStep(x=>x+1)}>Next<ArrowRightIcon data-icon="inline-end"/></Button>}</DialogFooter></DialogContent></Dialog>}
